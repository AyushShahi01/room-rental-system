class OnboardingModel {
  final String title;
  final String description;
  final String imagePath; // using an icon or image path

  OnboardingModel({
    required this.title,
    required this.description,
    required this.imagePath,
  });
}
